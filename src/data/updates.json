{
  "generatedAt": "2026-08-30T18:37:01.811Z",
  "source": "https://github.com/raultov/knot + https://github.com/raultov/knot-server",
  "entries": [
    {
      "repo": "knot",
      "version": "1.7.2",
      "title": "Deterministic Subgraph Root Resolution & MSBuild/NuGet Build-System Support",
      "date": "2026-08-30",
      "summary": "Part A: deterministic subgraph root resolution (the `get_entity_subgraph`",
      "highlights": [
        "New pure `root_kind_rank(kind)` precedence in",
        "New `resolve_subgraph_root(name, repo_name)` method on",
        "`get_entity_subgraph` now anchors the traversal on the",
        "`get_entity_subgraph` now sorts the collected nodes"
      ],
      "changelogUrl": "https://github.com/raultov/knot/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot-server",
      "version": "0.3.6",
      "title": "",
      "date": "2026-08-30",
      "summary": "**Worker status claim revert and atomic multi-instance registry updates:** (a) worker no longer leaves a claimed `cloning`/`pulling` status stuck when the repo lock is held by another node (the 202 sync could silently no-op), and (b) regist",
      "highlights": [
        "**Worker status claim revert and atomic multi-instance registry updates:** (a) worker no longer leaves a claimed `cloning`/`pulling` status stuck when the repo lock is held by another node (the 202 sync could silently no-op), and (b) registry mutations now re-read repos.json under the workspace lock so multi-instance deployments can no longer lose status/last_indexed updates (last-writer-wins full-file overwrites).",
        "**Nested types invisible in the graph overview:** nested declarations (inner classes, C# nested records/enums) were unreachable in the graph overview because the traversal closure started from `CONTAINS`-free roots and followed outgoing relationships only. The overview now merges in a one-hop `CONTAINS` query so every visible-kind declaration is present; node counts grow (openlogi-net 335→400, csharp-code-map 687→710); `fetch_edges` and the response schema are untouched.",
        "**C# kinds categorised (graph overview was empty for C# repos):** the C# support recently added to the knot library emits `csharp_*` entity kinds, but none of them were present in `KIND_CATEGORY_CLASSES`, `KIND_CATEGORY_INTERFACES` or `KIND_CATEGORY_FUNCTIONS` in `src/handlers/models.rs`, so `parse_kinds(\"classes,interfaces\")` matched nothing and `GET /api/repos/{id}/graph` returned an empty graph for every indexed C# repository. All C# declaration kinds are now categorised — types (`csharp_class`, `csharp_struct`, `csharp_record`, `csharp_enum`, `csharp_delegate`) in `classes`, `csharp_interface` in `interfaces`, and members (`csharp_method`, `csharp_constructor`, `csharp_local_function`, `csharp_operator`, `csharp_indexer`, `csharp_property`, `csharp_field`, `csharp_event`, `csharp_constant`) in `functions`. `assets/graph-viewer.html` gained matching `KIND_COLORS` entries (rebuild required, the file is embedded via `include_str!`). `csharp_namespace` intentionally stays uncategorised (only reachable via the `other` category): knot does not emit `namespace CONTAINS type` edges and namespace nodes carry almost no CALLS/EXTENDS/IMPLEMENTS edges, so they would otherwise flood the default overview with isolated containers. Four C# drift-guard unit tests added; the existing `kind_categories_are_disjoint_and_have_no_duplicates` test still passes (no kind belongs to two categories)."
      ],
      "changelogUrl": "https://github.com/raultov/knot-server/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot-server",
      "version": "0.3.4",
      "title": "",
      "date": "2026-08-28",
      "summary": "Added `GET /api/repos/{id}/graph/repos` to query repository-level dependency graphs (`DEPENDS_ON` relations).",
      "highlights": [
        "Added `GET /api/repos/{id}/graph/repos` to query repository-level dependency graphs (`DEPENDS_ON` relations).",
        "Added \"Repo Deps\" toggle and direction selector to the 3D graph viewer to visually explore codebase dependencies/dependents across the indexed ecosystem.",
        "Added an option to transition/explore another repository directly from the details panel when selecting dependency nodes.",
        "Added the new endpoint request to `knot-server.postman_collection.json`."
      ],
      "changelogUrl": "https://github.com/raultov/knot-server/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot-server",
      "version": "0.3.3",
      "title": "",
      "date": "2026-08-28",
      "summary": "**Fixed the `/index` skill:** the registration payload sent `\"auth_type\": {\"type\": \"none\"}`, an object form that the API never accepted (`AuthType` only deserializes the plain strings `\"ssh\"` and `\"https\"`), so every registration attempt fa",
      "highlights": [
        "**Fixed the `/index` skill:** the registration payload sent `\"auth_type\": {\"type\": \"none\"}`, an object form that the API never accepted (`AuthType` only deserializes the plain strings `\"ssh\"` and `\"https\"`), so every registration attempt failed with `422 Unprocessable Entity`. The skill now omits `auth_type` (optional, defaults to `\"ssh\"`) and documents the accepted values. Also corrected the same broken payload in the `repos` skill."
      ],
      "changelogUrl": "https://github.com/raultov/knot-server/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot",
      "version": "1.6.2",
      "title": "Accurate Indexing Progress",
      "date": "2026-08-15",
      "summary": "Indexing progress now reflects the **whole pipeline**, not just file reading.",
      "highlights": [
        "The percentage now spans the entire run via weighted bands:",
        "`IndexingProgress` exposes `total_entities: u64` (and the",
        "New `ParseCallbacks` struct replaces the v1.6.1",
        "New unit tests cover the banded formula edge cases"
      ],
      "changelogUrl": "https://github.com/raultov/knot/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot-server",
      "version": "0.3.2",
      "title": "",
      "date": "2026-08-15",
      "summary": "**Bump `knot` to v1.6.2:** Upgraded the core library dependency to include the latest fixes and additions (including `total_entities` field for indexing progress).",
      "highlights": [
        "**Bump `knot` to v1.6.2:** Upgraded the core library dependency to include the latest fixes and additions (including `total_entities` field for indexing progress)."
      ],
      "changelogUrl": "https://github.com/raultov/knot-server/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot-server",
      "version": "0.3.1",
      "title": "",
      "date": "2026-08-13",
      "summary": "**Bump `knot` to v1.6.1:** Upgraded the core library dependency to include the latest fixes from knot.",
      "highlights": [
        "**Bump `knot` to v1.6.1:** Upgraded the core library dependency to include the latest fixes from knot."
      ],
      "changelogUrl": "https://github.com/raultov/knot-server/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot",
      "version": "1.6.1",
      "title": "Varnish VCL Include Resolution",
      "date": "2026-08-12",
      "summary": "Resolved an issue where Varnish `include` directives with absolute paths failed to map to their target files.",
      "highlights": [
        "Resolved an issue where Varnish `include` directives with absolute paths failed to map to their target files. The parser now preserves raw path strings and the resolver uses a multi-strategy approach (repo-root fallback, relative path fallback, and filename fuzzy match) to reliably build the `INCLUDES` relationship.",
        "Expanded Varnish E2E integration tests with fixtures for absolute path resolution (`/etc/varnish/language.vcl`).",
        "Removed completed items (\"Varnish VCL support\" and CLI commands) from the README roadmap. Added the `varnish_include_resolution_plan.md` spec."
      ],
      "changelogUrl": "https://github.com/raultov/knot/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot",
      "version": "1.7.1",
      "title": "find_callers Target Resolution & Substring Noise Reduction",
      "date": null,
      "summary": "Implement a two-stage target resolution ladder for `find_references` / `find_callers`.",
      "highlights": [
        "Implement a two-stage target resolution ladder for `find_references` / `find_callers`. Matching is precedence-based: exact FQN → FQN suffix (`Type.member`) → exact name → signature prefix (`accept(List`) → fuzzy substring. An exact match now suppresses all fuzzy substring noise. **Behavior Change**: Queries that previously returned fuzzy noise alongside an exact hit now return only the exact hit.",
        "Add `entity_name_text` and `entity_fqn_text` `TEXT` indexes to Neo4j to speed up `ENDS WITH` and `CONTAINS` queries.",
        "Surface resolution metadata (tier matched, fuzzy warning, truncation notice) in both the CLI table formatter and markdown formatter."
      ],
      "changelogUrl": "https://github.com/raultov/knot/blob/master/CHANGELOG.md"
    },
    {
      "repo": "knot",
      "version": "1.7.0",
      "title": "C# Language Support",
      "date": null,
      "summary": "Closes [#5 — help wanted: add C# support](https://github.com/raultov/knot/issues/5).",
      "highlights": [
        "Full C# extraction (`.cs`) via `tree-sitter-c-sharp 0.23`.",
        "Namespace-qualified FQNs — `<namespace>.<Type>.<member>`",
        "`base_list` disambiguation heuristic — interfaces always",
        "Reference extraction with two C#-specific refinements:"
      ],
      "changelogUrl": "https://github.com/raultov/knot/blob/master/CHANGELOG.md"
    }
  ]
}
