# eslint-plugin-react@7.37.5 still declares a peer range capped at eslint ^9.7,
# but the project runs eslint 10 and lint passes cleanly. Accept the stale peer
# range so `npm install` works out-of-the-box (CI/deploy) without a manual flag.
legacy-peer-deps=true
